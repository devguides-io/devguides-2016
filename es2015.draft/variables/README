<h1 id="block-scoping">Block scoping</h1>
<p><code>let</code> works just like <code>var</code>. In most cases, you can use <code>let</code> instead of var.</p>
<pre><code class="language-js">function greet (user) {
  let name = user.name
  console.log('Hello, ' + name)
}
</code></pre>
<ul>
<li></li>
</ul>
<blockquote>
<p>But what makes it different? <a href="let-vs-var.md">Continue &gt;</a></p>
</blockquote>
