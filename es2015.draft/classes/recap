<h1 id="recap">Recap</h1>
<pre><code class="language-js">class Circle extends Shape {
  constructor (radius) {
    this.radius = radius
  }

  // Methods
  getArea () { return Math.PI * 2 * this.radius }

  // Static methods
  static createFromDiameter (d) { return new Circle(d / 2) }
}

c = new Circle(10)
</code></pre>
<ul>
<li></li>
</ul>
<blockquote>
<p>Let's learn about strings. <a href="../strings/README.md">Continue &gt;</a></p>
</blockquote>
