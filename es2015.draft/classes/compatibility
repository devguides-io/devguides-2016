<h1 id="compatibility">Compatibility</h1>
<p>Classes are simply syntactic sugar over ES5's prototypes. In fact, these two are equivalent:</p>
<pre><code class="language-js">class Circle {
  constructor (r) { this.radius = r }
  getDiameter () { return this.radius * 2 }
}
</code></pre>
<pre><code class="language-js">function Circle (r) { this.radius = r }
Circle.prototype.getDiameter = function () { return this.r * 2 }
</code></pre>
<p>You can both use them like so:</p>
<pre><code class="language-js">c = new Circle(20)
</code></pre>
<ul>
<li></li>
</ul>
<blockquote>
<p>Let's recap what we've learned. <a href="recap.md">Continue &gt;</a></p>
</blockquote>
